namespace NSoft.NFramework.Numerics.Signals {
    /// <summary>
    ///  Generic Function Sampling and Quantization Provider
    /// </summary>
    public static partial class SignalGenerator {
        #region << logger >>

        private static NLog.Logger log = NLog.LogManager.GetCurrentClassLogger();
        private static bool IsTraceEnabled = log.IsTraceEnabled;
        private static bool IsDebugEnabled = log.IsDebugEnabled;

        #endregion
    }
}